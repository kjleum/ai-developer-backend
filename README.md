# 🤖 AI Developer Platform v4.0

**Полноценная AI-платформа для разработки любых проектов — от простых API до enterprise SaaS.**

## ✨ Возможности

### 1. 🤖 Языковые Модели (LLM)
- **9+ облачных провайдеров**: Groq, Gemini, Together, DeepSeek, Mistral, Cohere, AI21, OpenRouter, HuggingFace
- **Локальные модели**: Ollama (Llama 3, Qwen, DeepSeek-Coder, Mistral)
- **Возможности**: Chat, Code Generation, Vision, Embeddings, Function Calling
- **Стриминг**: WebSocket поддержка для real-time генерации

### 2. 🎨 Генерация Медиа
- **Изображения**: Stable Diffusion (Automatic1111, ComfyUI), Kandinsky 3.0/4.0
- **Видео**: Wan-2.1 (14B), Kandinsky Video Lite
- **Аудио**: TTS (Coqui, Silero), STT (Whisper, Vosk), клонирование голоса
- **Функции**: Inpainting, Outpainting, ControlNet, LoRA, upscale

### 3. 🧠 AI Агенты
- **5 готовых агентов**: Developer, Analyst, Support, Manager, Creative
- **Инструменты**: поиск, код, уведомления, БД, API, файлы, команды
- **Интеграции**: Flowise (визуальные пайплайны), Activepieces (автоматизация)
- **Память**: контекстные диалоги с историей

### 4. 📚 RAG Система
- **Векторные БД**: Chroma, Qdrant, pgvector
- **Возможности**: индексация документов, семантический поиск, чат с документами
- **Эмбеддинги**: Cohere, Giga Embeddings (Сбер), Ollama local

### 5. 💬 NLP Интерфейс
- **Управление БД голосом**: "создай таблицу клиентов", "покажи заказы"
- **Аналитика**: "график продаж", "отчёт по просрочкам"
- **AI-понимание**: естественный язык → SQL/действия

### 6. 🏗 Генерация Проектов
- **Типы**: API, Bot, Frontend, Fullstack, SaaS, Marketplace, CRM, ERP
- **Технологии**: FastAPI, Next.js, React, PostgreSQL, Prisma, Docker
- **Инфраструктура**: Docker Compose, CI/CD, тесты, документация
- **Деплой**: автоматический на Render + GitHub

## 🚀 Быстрый Старт

### Локальный запуск

```bash
# 1. Клонирование
git clone https://github.com/yourusername/ai-developer-platform.git
cd ai-developer-platform

# 2. Установка зависимостей
pip install -r requirements.txt

# 3. Настройка окружения
cp .env.example .env
# Отредактируй .env — добавь хотя бы один AI API ключ

# 4. Запуск
python main.py
```

### Docker (рекомендуется)

```bash
# Полный стек с векторными БД и медиа-сервисами
docker-compose up -d

# Только backend
docker-compose up -d app db
```

## 📚 Документация API

После запуска доступна интерактивная документация:
- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

### Примеры запросов

**Генерация кода:**
```bash
curl -X POST http://localhost:8000/ai/generate \
  -H "Content-Type: application/json" \
  -d '{
    "prompt": "Напиши FastAPI endpoint для загрузки файлов",
    "provider": "groq",
    "temperature": 0.3
  }'
```

**Создание проекта:**
```bash
curl -X POST http://localhost:8000/projects \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": "user_123",
    "config": {
      "name": "CRM System",
      "type": "crm",
      "description": "CRM для управления клиентами",
      "features": [
        {"name": "Управление контактами", "priority": "must"},
        {"name": "Воронка продаж", "priority": "must"}
      ]
    }
  }'
```

**Чат с агентом:**
```bash
curl -X POST http://localhost:8000/agents/developer/run \
  -H "Content-Type: application/json" \
  -d '{"input": "Создай API для блога на FastAPI"}'
```

## 🏗 Архитектура

```
ai-developer-platform/
├── backend/
│   ├── core/
│   │   ├── ai_manager.py        # Управление LLM
│   │   ├── media_processor.py   # Изображения/видео/аудио
│   │   ├── agent_system.py      # AI агенты
│   │   ├── rag_engine.py        # Векторный поиск
│   │   ├── nlp_interface.py     # NLP для БД
│   │   ├── project_builder.py   # Генерация проектов
│   │   ├── deploy_engine.py     # Деплой
│   │   └── database.py          # Работа с БД
│   └── models/
│       └── schemas.py           # Pydantic модели
├── frontend/                    # Telegram Mini App
├── docker/                      # Конфигурации Docker
└── scripts/                   # Утилиты установки
```

## 🔧 Настройка Компонентов

### Локальные AI Модели (Ollama)

```bash
# Установка Ollama
curl -fsSL https://ollama.com/install.sh | sh

# Загрузка моделей
ollama pull llama3.3
ollama pull qwen2.5
ollama pull deepseek-coder-v2
ollama pull nomic-embed-text  # для эмбеддингов
```

### Stable Diffusion

```bash
# Automatic1111
git clone https://github.com/AUTOMATIC1111/stable-diffusion-webui.git
cd stable-diffusion-webui
./webui.sh --api --listen
```

### Векторные БД

**Chroma (по умолчанию):**
```bash
docker run -p 8000:8000 chromadb/chroma:latest
```

**Qdrant:**
```bash
docker run -p 6333:6333 qdrant/qdrant:latest
```

## 📝 Переменные Окружения

| Переменная | Описание | Обязательная |
|------------|----------|--------------|
| `GROQ_API_KEY` | Groq API (быстрый Llama) | Нет (рекомендуется) |
| `GEMINI_API_KEY` | Google Gemini | Нет |
| `DATABASE_URL` | PostgreSQL для данных | Да |
| `SUPABASE_URL` | Supabase для проектов | Нет |
| `GITHUB_TOKEN` | Для авто-деплоя | Нет |
| `OLLAMA_URL` | Локальные модели | Нет |

## 🤝 Интеграции

- **GitHub**: Автоматический push и деплой
- **Render**: Хостинг с автоскейлингом
- **Supabase**: База данных и аутентификация
- **Flowise**: Визуальные LLM-пайплайны
- **Activepieces**: Автоматизация workflows
- **Telegram**: Уведомления и Mini App

## 🛡 Безопасность

- Все API ключи хранятся в `.env`
- CORS настроен для безопасности
- Rate limiting рекомендуется (nginx/cloudflare)
- Не коммитьте `.env` в git!

## 📈 Масштабирование

**Малый масштаб (1-10 пользователей):**
- SQLite + локальные модели
- Один Docker container

**Средний масштаб (10-1000):**
- PostgreSQL + Redis
- Chroma/Qdrant для RAG
- Render Standard tier

**Enterprise (1000+):**
- Kubernetes кластер
- Микросервисная архитектура
- Выделенные GPU для AI

## 📄 Лицензия

MIT License — свободное использование для любых целей.

## 🙏 Благодарности

- [FastAPI](https://fastapi.tiangolo.com/) — веб-фреймворк
- [Ollama](https://ollama.com/) — локальные LLM
- [Chroma](https://www.trychroma.com/) — векторная БД
- [Stable Diffusion](https://stability.ai/) — генерация изображений

---

**Создано с ❤️ и 🤖**
